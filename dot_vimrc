:set number
:set relativenumber
:imap kj <Esc>
:set so=999
:set cursorline
:set cursorcolumn
:set ignorecase
:set smartcase
set virtualedit=all
if !has('nvim')
  call plug#begin()
    Plug 'mtdl9/vim-log-highlighting'
  call plug#end()
endif
